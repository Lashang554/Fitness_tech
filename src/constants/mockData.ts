import type { Workout } from '../types/workout.types';

export const mockWorkouts: Workout[] = [
  {
    id: 'morning-yoga',
    title: 'Morning Yoga',
    durationMinutes: 20,
    intensity: 'Low',
  },
];
